/*
So kind of like lod but just do it from scratch
say 10kx10k and every 100 blocks is sampled
just uses 2d height function

initmap  height_fn, s, f -> mesh
draw as per lodmesh basically but different fog etc and camera obviously
*/